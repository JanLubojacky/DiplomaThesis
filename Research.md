# Model evaluation

**feature selection**
- some dumb feature selection such as variation filtering that can be performed at the start
- run k-fold cross validation, run feature selection in each fold, this gives a rank to each feature, in the end select the features which have consistently low ranks across multiple folds

**hyperparameter optimization**
- use optuna
- 
